apiVersion: v1
kind: Service
metadata:
  name: laravel
  labels:
    app: laravel
    tier: frontend
spec:
  type: LoadBalancer
  ports:
    - port: 80
  selector:
    app: laravel
    tier: frontend
